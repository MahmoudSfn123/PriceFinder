   <!-- single home slider -->
   <div class="{{$img}}">
    <div class="container">
        <div class="row">
            <div class="{{$position}}">
                <div class="hero-text">
                    <div class="hero-text-tablecell">
                        <p class="subtitle ">{{$subtitle}}</p>
                        <h1>{{$header}}</h1>
                        <div class="hero-btns">
                            <a href={{route('products.create')}} class="boxed-btn">{{$boxedbtn}}</a>
                            <a href="contact.html" class="bordered-btn">{{$borderedbtn}}</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

